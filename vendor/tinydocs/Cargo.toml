[package]
name = "tinydocs"
version.workspace = true
edition = "2024"
rust-version = "1.88"
license = "GPL-3.0-only"
description = "Agent-friendly document synthesis and text extraction (DOCX, PPTX, PDF) in Rust."
repository = "https://github.com/tinyhumansai/tinydocs"
documentation = "https://docs.rs/tinydocs"
readme = "README.md"
keywords = ["docx", "ooxml", "pdf", "document", "agent"]
categories = ["text-processing"]
# Keep the published package to what a consumer actually needs.
exclude = [
    ".github/",
    ".gitmodules",
    "docs/",
    "vendor/",
    "worktrees/",
    ".env.example",
    "deny.toml",
]

[workspace]
members = ["crates/tinydocs-bus", "crates/tinydocs-module"]
default-members = [".", "crates/tinydocs-bus", "crates/tinydocs-module"]
exclude = ["vendor/tinybus"]
resolver = "3"

[workspace.package]
# Releases update this sole workspace version before packaging every crate.
version = "0.1.14"

[dependencies]
# The dependency-light TinyBus contract owns payload types and errors. Re-export
# it so library and bus callers use the exact same values.
tinydocs-bus = { version = "0.1.13", path = "crates/tinydocs-bus" }
# OOXML `.docx` synthesis. Optional: exclusive to the `docx` feature so a host
# that only needs extraction does not pull the writer stack.
docx-rs = { version = "0.4.20", optional = true }
# OOXML `.pptx` synthesis. Optional: exclusive to the `pptx` feature.
#
# Default features are ON, and not by choice. `pdf-native` exists to export a
# deck as PDF and pulls `pdfrs` -> `syntect` -> `yaml-rust`, which is a syntax
# highlighter this crate never reaches — but ppt-rs 0.2.24 does not compile with
# it off: `src/export/slide_render.rs` has an unguarded `use pdfrs::...` with no
# `#[cfg(feature = "pdf-native")]`. Turning the feature off fails to build the
# dependency, not our code.
#
# The cost is three RUSTSEC *unmaintained* advisories carried transitively,
# ignored with justification in `deny.toml`. Revisit the moment ppt-rs fixes the
# cfg: `default-features = false` then drops `pdfrs`, `syntect`, `yaml-rust`,
# `bincode` and `pulldown-cmark` in one line (verified locally — the resolve
# succeeds, only the build fails).
ppt-rs = { version = "0.2", optional = true }
# `.pdf` text extraction. Optional: exclusive to the `pdf` feature. It brings a
# font and PostScript parsing stack (`lopdf`, CFF/Type1/CMap parsers) that only
# the extraction path needs, so a host that never reads a PDF should not carry
# it.
pdf-extract = { version = "0.12", optional = true }

[dev-dependencies]
# `.docx` output is a zip container; the tests re-open the produced bytes and
# assert on the OOXML parts inside.
zip = { version = "8", default-features = false, features = ["deflate"] }
# The spec types are a JSON wire contract; the tests assert they round-trip and
# that unknown keys are rejected.
serde_json = "1"

# The example generates a `.docx`, so it only builds when that gate is on.
# Without this, `--no-default-features` fails on the example rather than
# reporting the (correct) fact that the crate itself compiles fine.
[[example]]
name = "basic"
required-features = ["docx"]

[features]
default = ["docx", "pptx", "pdf"]
# `.docx` generation via `docx-rs`.
docx = ["dep:docx-rs"]
# `.pptx` generation via `ppt-rs`.
pptx = ["dep:ppt-rs"]
# `.pdf` text extraction via `pdf-extract`.
pdf = ["dep:pdf-extract"]

# Lints apply to the whole crate and to every target. CI runs clippy with
# `-D warnings`, so anything set to "warn" here fails the build in CI.
[workspace.lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
missing_debug_implementations = "warn"
unreachable_pub = "warn"
rust_2018_idioms = { level = "warn", priority = -1 }

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
# Library code must not panic on its own; tests and examples may.
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
todo = "warn"
unimplemented = "warn"
# Public fallible/panicking APIs must document their failure modes.
missing_errors_doc = "warn"
missing_panics_doc = "warn"
# Documentation hygiene.
doc_markdown = "warn"
# `#[must_use]` on pure public functions.
must_use_candidate = "warn"

[workspace.lints.rustdoc]
broken_intra_doc_links = "warn"
private_intra_doc_links = "warn"

[lints]
workspace = true

[profile.release]
# Cross-crate optimization and smaller, faster binaries for release builds.
lto = "thin"
codegen-units = 1
strip = "debuginfo"
