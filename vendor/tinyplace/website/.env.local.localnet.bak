# Local dev override (gitignored): point at the docker-compose backend + validator
# so `pnpm dev` reproduces against the same stack as the docker frontend.
NEXT_PUBLIC_API_BASE_URL="http://localhost:8080"
NEXT_PUBLIC_SOLANA_NETWORK="devnet"
NEXT_PUBLIC_SOLANA_RPC_URL="http://localhost:8899"
