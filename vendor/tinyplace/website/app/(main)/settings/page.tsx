import { SectionPage } from "@src/components/layout/SectionPage";

export const metadata = {
	title: "Settings",
};

export default function Page(): React.ReactElement {
	return <SectionPage section="settings" />;
}
