# Generated by Cargo
# will have compiled files and executables
/target/
# The release build's own target dir (see scripts/release-neppy.sh). A root
# .gitignore rule cannot cover this: a pattern containing a slash is anchored to
# its own directory, so `src-tauri/target-release/` there never matched this path.
/target-release/
/binaries/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
